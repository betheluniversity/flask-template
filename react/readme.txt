Optional: React project files go here, and the build and compile into the app/static folder as needed.

Flask routes may need to be adjusted to accomodate a frontend layer (react) and an API layer (flask)